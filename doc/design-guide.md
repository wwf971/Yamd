
For a file `Abc.tsx` implementing component `Abc`, keep the component definition close to the head of the file, and place utility functions at the end of the file.

TypeScript in this project should stay lightweight.

- Use TypeScript mainly for important data contracts, store state shape, and key event structures.
- Avoid over-typing local UI details and temporary variables.
- Favor readable code and fast iteration over strict type-heavy patterns.
